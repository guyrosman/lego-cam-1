# Runtime pip dependencies.
#
# NOTE: On Raspberry Pi OS, Picamera2 is typically installed via apt:
#   sudo apt install -y python3-picamera2
#
# ffmpeg is also typically installed via apt:
#   sudo apt install -y ffmpeg

# Used by vision motion detection (if installed; otherwise it auto-disables).
numpy>=1.26

# Optional: only needed if you use .yaml/.yml config files.
PyYAML>=6.0

# TMF8820/TMF8821 Time-of-Flight sensor driver (used with SparkFun TMF8820 board).
tmf882x-driver>=0.1.0

